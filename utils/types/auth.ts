export type SignInInputTypes = {
  email: string;
  password: string;
  remember?: boolean;
};

export type SignUpInputTypes = {
  email: string;
  name: string;
  password: string;
  terms: boolean;
};
